apiVersion: v1
kind: Secret
metadata:
  name: dozerdb-auth
  namespace: literag
  labels:
    app: dozerdb
type: Opaque
# Dev placeholder — override with a real credential before deploying:
#   kubectl -n literag create secret generic dozerdb-auth \
#     --from-literal=NEO4J_AUTH='neo4j/<strong-password>'
stringData:
  NEO4J_AUTH: neo4j/password
