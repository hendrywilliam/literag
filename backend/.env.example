# Server
PORT=8080

# RAG (FastAPI) service
RAG_URL=http://localhost:8000

# LLM provider (OpenRouter, OpenAI-compatible)
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_API_KEY=
OPENROUTER_MODEL=deepseek/deepseek-v4-flash-latest

# PostgreSQL (repo layer — backlog)
DATABASE_URL=
